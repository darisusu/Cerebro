import SwiftUI

struct ExitView: View {
    var body: some View {
        VStack(spacing: 20) {
            Text("See you tomorrow!")
                .font(.title2)

            Button("Exit App") {
                // Placeholder: Apple Vision Pro apps don't exit manually
            }
        }
        .padding()
    }
}

